FCJA Agendamentos — Web + Desktop (with admin login)
Run desktop admin (Tkinter):
    python admin_desktop.py
Default admin/admin
Run web app:
    cd web
    python app.py
Install: pip install -r requirements.txt

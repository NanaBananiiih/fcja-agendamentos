# Banco (opcional no runtime: só precisa se for usar get_connection()/init_db)
python-dotenv==1.0.1

# OBS: psycopg2-binary costuma dar problema em Windows/py3.10+.
# Se der erro ao instalar, comente esta linha e veja as instruções abaixo.
psycopg2-binary==2.9.10

# Supabase (cliente HTTP)
supabase==2.25.0

# Interface gráfica
ttkbootstrap==1.7.3
tkcalendar==1.6.1
Pillow==9.5.0

# Web (Flask e dependências relacionadas)
Flask==3.0.3
Werkzeug==3.1.3
Jinja2==3.1.4
click==8.1.7
itsdangerous==2.2.0
MarkupSafe==2.1.5
packaging==24.1

# Deploy (somente no servidor/web)
gunicorn==23.0.0
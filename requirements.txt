# Banco (opcional no runtime: só precisa se for usar get_connection()/init_db)
psycopg2-binary==2.9.10
python-dotenv==1.0.1

# Supabase (versão estável compatível com dependências)
supabase==2.25.0

# Interface gráfica
ttkbootstrap==1.7.3
tkcalendar==1.6.1
Pillow==9.5.0

# Web (Flask)
Flask==3.0.3
Werkzeug==3.1.3
Jinja2==3.1.4
click==8.1.7
itsdangerous==2.2.0
MarkupSafe==2.1.5
packaging==24.1

# Deploy
gunicorn==23.0.0